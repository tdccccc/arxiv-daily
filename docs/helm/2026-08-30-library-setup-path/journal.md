## 2026-08-30 — start

- evidence: PR 40 已合入 main。用户反馈不是检索引擎本身，而是选库 → 嵌入 → 授权 → 索引 → 搜索被拆成五段，建索引只在命令面板。现状：选文件夹后主按钮永远是 Review & authorize；授权后文案写「Metadata and abstracts authorized」；本地嵌入其实不需要授权也能 `indexPersonalLibraryFullText`。
- change: 新建本 goal。P1 只改设置页文献库一行的说明和主按钮。不撤回 PR 40。
- disposition: 检索、打开 PDF、generation 索引保持现状。发现闭环 / email helm 不改。
- next: 先写失败测试：本地模式下选文件夹后应出现 Build index，而不是必须授权。

## 2026-08-30 — P1 done, start P2

- evidence: 设置页文献库行按 embedding.mode 给出下一步。local + 已选文件夹主按钮为 Build index；remote 未授权为 Review & authorize，授权后为 Build index。Revoke 收回 Manage。plugin 设置/连接测试 62 项 + 新增判定 3 项与 typecheck 通过。
- change: P1 done，激活 P2。勾选成功标准 1–5。
- disposition: 命令面板索引入口保留。P2 再处理索引中的进度与「去 Dashboard 搜索」的可见下一步。
- next: P2 让建索引进行中与完成后的下一步可感知。

## 2026-08-30 — P1b compact settings, hold P2

- evidence: 用户反馈 Personal library 选项太多、挤在一起。默认 local 下仍能看到远程 URL/key/dimension 和 sidecar URL；Embedding / PDF parsing 还是独立分组，legacy display() 顺序也和 1.13+ 不一致。
- change: Embedding 与 Better PDF parser 并进 Personal library，放到 LLM 后面。默认三行：Library（主按钮）、Embedding 下拉、Better PDF parser 开关。远程字段仅 remote 时出现，sidecar URL 仅打开后出现。按钮一行右对齐。P2 索引进度改 pending，等用户看过这页再做。
- disposition: 不撤回 PR 40。不改检索、段落证据、generation。发现闭环 / email helm 不改。
- next: 用户在 plugin_test 重载后看设置页；通过再做 P2。

## 2026-08-30 — P1c drop Manage menu

- evidence: Library 行仍是三按钮 + 一个 Manage… 二级菜单（Review directions / Preview library files / Scan library / Reload catalog / Revoke authorization）。用户决策：彻底去掉 Manage，不要二级菜单。
- change: 删除 Manage… 按钮与 `openLibraryManageMenu`；授权状态下 Revoke 变成 Library 行上的普通按钮（非 CTA）。原菜单另外四项移到命令面板：方向复审复用已有 `review-personal-library-directions`；新增 `preview-personal-library-files`、`scan-personal-library`（Scan personal library folder (rebuild catalog)）、`reload-personal-library-catalog`（Reload personal library catalog from disk）。读过 main.ts 后确认 scan 与 reload 不等价——scan 走文件夹重新识别并重写目录（有网络与进度），reload 只把存盘目录读回内存——故注册两条命令而非一条。设置页里已无人调用的 preview / scan / reload 包装方法删掉，plugin 主类方法原样保留。
- disposition: `renderLibraryConnectionControls` 被 declarative 与 legacy display() 共用，只改这一处即覆盖两条路径。不改检索排序、段落证据、generation。发现闭环 / email helm 不动。工作区保持未提交，叠在 P1b 之上。
- next: 用户重载后确认 Library 行只剩目标按钮；若找不到扫描/重载入口，在行下加指向命令面板的说明文字，而不是把 Manage 加回来。

## 2026-08-30 — P1c 收尾：Personal library 整块下移

- evidence: 用户理由是排序问题而非内容问题——文献库是附加功能，不该排在主流程（LLM → 分类 → 主题 → 输出与排程）前面。此前 P1b 把它放在 LLM 之后，等于让附加功能挡在必填项中间。分组顺序在两条渲染路径上各写一份：declarative 走 `definitions.ts` 的 `buildSettingDefinitions` 数组字面量顺序，legacy 走 `tab.ts` `display()` 里 `sectionHeading` 调用的语句先后。P1c 主体那次共用 `renderLibraryConnectionControls` 所以一处即可，这次不共用。
- change: Personal library 整块（Library 行、Embedding 下拉、远程嵌入四字段、Better PDF parser 开关、两个 sidecar 字段）原样搬到 Output & schedule 之后、Email delivery 之前，两条路径分别改。块内行内容、条件显隐、按钮、文案一字未动。
- disposition: 两条路径最终顺序一致：LLM / arXiv / Research topics / Output & schedule / Personal library / Email delivery / Advanced / Help & feedback（legacy 首段标题仍写 "AI model"、分类段仍写 "arXiv"，本次不动措辞）。不改检索排序、段落证据、generation。发现闭环 / email helm 不动。工作区保持未提交。
- next: 用户在 plugin_test 重载后确认文献库落在排程与邮件之间；若仍觉得偏靠前，下一步是折叠为默认收起的区块，而不是继续挪位置。

## 2026-08-30 — P1d 远程授权改为就地确认

- evidence: 用户认为「我都选了这个目录了，还要我授权？」—— 选目录这个动作本身已经表达了同意，Library 行上再放一个 Review & authorize 按钮像是把同一件事问两遍。但把全文发到第三方嵌入接口和本地读文件不是同一件事（ADR 0005 §5 明确把 connection 与 model authorization 分开），所以最终决定是保留授权、只改询问时机（方案 A + A1）。
- change: Library 行的主按钮只要选了文件夹永远是 Build index，Review & authorize 按钮删除；`librarySetupNextStep` 去掉 `authorize` action，改成 `index` + `remoteConsentPending` 标志，按钮层拿不到授权分支，描述文字仍能区分远程未授权/已授权/指纹失效。全部询问收敛到一个 `requestRemoteFullTextConsent()`（granted / declined / undisclosable），由四个时机先到先弹：切到 remote（确认后才落 mode 再写授权，取消一个字节都不改）、选完文件夹（切换时没有文件夹可披露就推迟到这里）、改动已授权接口（指纹失配就重问，取消恢复已授权的值）、remote 未授权时点 Build index（遗留配置兜底，取消中止建索引）。Revoke 走 A1：先确认「撤销授权并切回本地嵌入」并说明已有索引失效需重建，确认后撤销 + mode 回 local，因此不存在「remote 但未授权」的死状态。顺带修了两个披露不实的问题：`libraryAuthorizationDisclosure` 的 depth 原本取自既有 grant，首次远程授权前会把 full-text 披露成 metadata-only，改为取自当前 scope；嵌入接口地址原本复用 chat 的 URL 构造，弹窗里写的是 `.../chat/completions`，而全文实际被 POST 到 `{baseUrl}/embeddings`，改成显示真正的目的地（脱敏逻辑不变，指纹计算不受影响，存量 grant 不失效）。披露文案补上用途（相似度向量）、其它内容不离开本机、可随时撤销。
- disposition: **ADR 0005 / 0008 不改**。授权的四个要素一个没动 —— 仍是全文深度（ADR 0008 §2）、仍披露具体接口地址、仍绑定接口指纹（ADR 0008 §5：改接口即重新询问）、仍可撤销；变的只是「什么时候问」，而 ADR 0005 §5 本来就把「settings 和 UI 怎么表现同意」列为 non-decision，所以这次改动落在 ADR 授权的自由度之内，不是推翻它。接口指纹的定义也保持不变（只含 baseUrl，不含 model）：model 不是目的地，把它塞进指纹会让所有存量 grant 一次性失效；重新询问的判定写成「保存后指纹是否失配」，将来若扩宽指纹定义会自动覆盖 model。不改检索排序、段落证据、generation。发现闭环 / email helm 不动。工作区保持未提交。
- validation: plugin 668 tests 全绿、typecheck、build、boundaries 通过，产物已覆盖到 plugin_test（md5 一致，main.js 晚于所有改动源文件）。全量 `npm test` 另有一条与本次无关的红：`packages/core/tests/pipeline/pipeline-novelty-stage.test.ts` 单跑即 heap OOM，属本分支已提交的 core 测试，本次未碰 packages/。
- next: 用户在 plugin_test 重载后确认：切 Embedding 下拉会弹披露、取消会弹回 local、Library 行只剩 Change folder / Build index / Revoke。若觉得「切下拉就弹框」太突兀，下一步是把披露并到 Build index 一个时机，而不是把授权按钮加回来。

## 2026-08-30 — P1c / P1d 的眼验改成桌面验收，几何断言当场抓到一个 CSS 缺陷

- evidence: P1c / P1d 收尾时两次都写着「用户在 plugin_test 重载后确认」——分组顺序、Library 行按钮、切下拉弹披露、取消回滚，全靠人开 Obsidian 看。这一步现在由 `2026-08-27-obsidian-desktop-acceptance-harness` 的 P5 场景在真实渲染进程里做掉：分组顺序（Output & schedule → Personal library → Email delivery 相邻）、Library 行按钮集合（local 与 remote 两态各一次，均为 Change folder + Build index，无 authorize、无 Manage）、切 Embedding 到 remote 就地弹披露、取消后下拉回 local 且 `embedding.mode` 与授权状态一字未变、remote 未授权时点 Build index 先弹披露且取消不开始索引（`operations.snapshot()` 为空、无「indexing…」通知）、全程零 console / pageerror。每条都取得了反向证据：篡改期望值后必定变红，失败信息回读页面实际值。
- change: 无产品代码改动。本条只记录验收归属的转移，以及下面这个发现。
- disposition: **发现一个本分支引入的布局缺陷，未修**。`09d43f1` 把 `.arxiv-daily-settings__library-controls` 从 `flex-wrap: wrap` 改成 `nowrap` 并加了 `min-width: 0`。`min-width: 0` 让控件盒可以收缩到按钮内容宽度以下，而按钮是 `flex: 0 0 auto` 不跟着缩，于是按钮整体向左溢出控件盒、压在 Library 行的描述文字上：1400px 窗口（面板 848px）下压进 13.6px，700px 窗口（面板 448px）下压进 118.2px，截图里能直接看到 "Change folder" 盖住 "Selected: small_library. Local embedding stays…"。`nowrap` 则在 Obsidian 自己的窄屏堆叠布局（窗口 < 700px）下更糟：那里 Obsidian 把每个控件拉成整行宽，三个按钮不换行地横铺出面板之外（窗口 560px、面板 322px 时按钮横跨 273..645）。这是 happy-dom 单元测试结构上测不到的一类问题，正是加桌面验收的理由。
- boundary: 桌面验收能证明的是**结构与几何**——哪些按钮在、顺序如何、对话框是否出现、取消是否真的什么都没改、按钮落在什么坐标。它证明不了**措辞和时机是否合理**：披露文案是否让人看懂全文会离开本机、「切下拉就弹框」是否突兀、Library 行的描述是否说清了下一步。这些仍需人看图判断，截图（`.acceptance-out/desktop-acceptance/`，git 忽略）就是为此而落的，不是为了自动比对。
- next: 由用户决定是否修 `.arxiv-daily-settings__library-controls`；修法上 `min-width: 0` 与 `flex-wrap: nowrap` 都要重新考虑，不能只调其一。修完重跑 `npm run test:desktop`，`library-row-geometry` 应转绿。

## 2026-08-30 — Library 行按钮溢出，修布局

- evidence: 缺陷是上一条记的那次桌面验收当场抓到的，不是人看出来的：`library-row-geometry` 在真实渲染进程里量到按钮左边界越过 `.setting-item-info` 的右边界——面板 848px 侵入 13.6px，面板 448px 侵入 118.2px，截图里 "Change folder" 直接盖在 "Selected: small_library. Local embedding stays…" 上。**单元测试为什么抓不到**：happy-dom 没有布局引擎，`getBoundingClientRect()` 一律返回零矩形，Obsidian 的 `app.css` 也不在测试环境里，所以「按钮在哪」这个问题在 happy-dom 里根本无法提问。当时替代它的是 `settings-tab.test.ts` 里一条对 `styles.css` 做正则的文本断言，它证明的是「我们写了 `flex-wrap: nowrap`」，不是「按钮排成了一行」；而那条声明恰恰是和 `min-width: 0` 一起加进来的，两条合起来正是缺陷本身。断言绿着，产品坏着。
- change: 根因两条，各修各的。其一，`.arxiv-daily-settings__library-controls` 上的 `min-width: 0` 删掉。Obsidian 自己的默认值本来就是对的——`.setting-item-info` 给 `min-width: 0`（文字列可以收缩重排），`.setting-item-control` 保持 `min-width: auto`（按钮列不收缩），行变窄时让文字让位。`min-width: 0` 把控件盒的自动最小尺寸从「按钮需要的宽度」降到 0，而按钮是 `flex: 0 0 auto` 不跟着缩，于是整组按钮向左溢出到描述文字上。改回默认后按钮保持自然宽度、描述文字重排，两者不重叠；`flex-wrap: nowrap` 与 `justify-content: flex-end` 原样保留，所以正常宽度下仍是一行右对齐，没有退回 P1b 之前的杂乱换行——真正让它不换行的是控件盒不收缩，不是那条 `nowrap`。另外把「文字列可以收缩」从依赖 Obsidian 默认改成本行显式声明（`:has(> .arxiv-daily-settings__library-controls)`），免得主题覆盖掉 `.setting-item-info` 时整行再次塌掉。其二，`nowrap` 限定在非堆叠布局下：Obsidian 的设置面板 `.vertical-tab-content` 是 `container-type: inline-size` 容器，`@container (max-width: 340px)` 下它把设置行变成一列、把每个按钮拉成整行宽——那里坚持一行只会把按钮推出面板（实测窗口 560px / 面板 322px 时按钮横跨 273..645，行本身只有 257..471，溢出 190px）。同名容器查询里改成 `flex-wrap: wrap`，按钮按 Obsidian 的本意竖着堆叠。按钮逻辑、文案、状态判定一字未动。
- disposition: 桌面验收 13 项全绿（原 12 项 + 新增的堆叠断言）。新增 `library-row-geometry-stacked`：堆叠档以前只测量不断言，现在断言按钮不溢出控件盒、不溢出行——这是加严不是放宽，加之前先确认它在修复前是红的（溢出 190px），修复后转绿（三按钮时堆成 3 行，都在行内）。堆叠档不能沿用 `library-row-geometry` 的判据：一旦 Obsidian 把每个按钮拉成整行宽，「同一行」和「右对齐」就不再有意义，能守的只剩「不越界」。三按钮状态（Change folder + Build index + Revoke）另跑一次授权后的探针验证：面板 848px 与 448px 一行右对齐不越界，面板 322px 堆成三行都在行内。
- **顺带修好了一个让上面这些证据本来不成立的洞**：桌面验收的 `deployBuildUnderTest` 只往测试库拷 `main.js` 和 `manifest.json`，不拷 `styles.css`。也就是说所有几何断言一直在量测试库里那份陈旧的样式表，而不是被测分支的。改完 CSS 后重跑，数字一个像素都没变，才发现这件事。已把 `styles.css` 加进部署产物与 preflight 检查（不是判据或容差的改动，是让被测对象真的是被测分支）。这也意味着上一条 journal 里记的那些溢出数字虽然结论正确，但严格说来量的是测试库里那份 CSS——所幸它与分支同源，结论不受影响。
- boundary: 这次只动样式与验收的部署链路。按钮集合、文案、`librarySetupNextStep` 的状态判定不动，ADR 0005 / 0008 不动。发现闭环 / email helm 不动。工作区保持未提交。
- validation: `npm run test:desktop` 13/13 PASS，零 console / pageerror；`npm test` 全工作区绿（plugin 670 tests / 42 files，core 各分片全绿）；`npm run test:release-tools` 221/221（含新增的部署与 preflight 覆盖）；`npm run check:boundaries` OK。截图重新落在 `.acceptance-out/desktop-acceptance/`，新增 `library-row-stacked-panel.png`。
- next: 一个留给用户判断的观察：三按钮状态在面板 448px（窗口 700px）下，三个按钮加间距要 302px，而行内可用宽度只有 308px，描述文字被挤成每行一个字（见 `.acceptance-out/three-button-probe/library-row-three-buttons-narrow.png`）。断言全绿——按钮没盖住文字、没越界——但可读性很差。要改善只能让这一档也换行，而那会和 `library-row-geometry` 现在要求的「面板 448px 一行右对齐」直接冲突，所以没有自行放宽，交由用户决定是否调整那一档的期望。

## 2026-08-30 — 三按钮状态的说明文字压成一列，改成「挤不下就换行」

- evidence: 上一条 next 里留给用户判断的那个观察，现在有了断言：面板 448px（窗口 700px）下，已授权的三按钮行（Change folder + Build index + Revoke）里，说明文字只剩 6px 宽，107 个字符被排成 13 行，一行一个字母（`.acceptance-out/three-button-probe/library-row-three-buttons-narrow.png`）。几何断言全绿——不重叠、不越界、右对齐——因为「没重叠」从来就不等于「读得了」。这正是 `phases/02-compact-library-settings.md` 里写下的中止条件「如果按钮在窄设置栏里被裁切到看不见主 CTA，改回允许换行」所指的那类失败，只是失败的不是 CTA 而是它旁边的句子。
- change: 只改样式。行内可用宽度在面板 448px 时是 308px，两个按钮加间距要 210.58px，也就是说这一档本来就只剩 81px 给文字——按钮组能占多少，是整件事唯一的变量。于是把它直接写成上限：`.arxiv-daily-settings__library-controls` 拿 `max-width: calc(100% - 72px - 16px)`（要留下的文字宽度 + Obsidian 两列之间的 16px 间距），剩下的自然就是那个下限。配套的 `flex-shrink: 0` 才是关键：`.setting-item-control` 默认 `flex: 1 1 auto`，若只加 `flex-wrap: wrap` 而任其收缩，浏览器会按两列的固有宽度比例分配缺口，把按钮组一路压到 min-content（最宽的那个按钮 110.67px），连两按钮档在 448px 都会换行。锁死收缩后按钮组只有两种宽度：自然的一行宽，或上限——换不换行由这两者是否相等决定，而不是由面板宽度阈值决定，后者根本不知道行里有几个按钮。三按钮档另给一条 `:has(> button:nth-of-type(3))`，上限收到 `calc(100% - 176px - 16px)`：448px 下按钮组只剩 116px，三个按钮各占一行右对齐，文字拿回 176px。堆叠档（`@container (max-width: 340px)`）把上限还原成 100%——那里 Obsidian 把行变成一列、按钮拉成整行宽，没有第二列要保护，留着上限只会把本该通栏的按钮缩窄。`.setting-item-info` 的 `min-width` 从 `0` 改成 `72px`，与上限两头对齐；它取在 81px 之下，所以永远不会反过来成为按钮换行的理由。按钮逻辑、文案、状态判定一字未动。
- disposition: 桌面验收从 13 项加到 15 项。新增 `library-row-three-buttons-geometry`（面板 848px / 448px）与 `library-row-three-buttons-geometry-stacked`（面板 322px），走真实披露框授权拿到 Revoke 按钮——从 Embedding 下拉切 remote 并点 Authorize，而不是点 Build index，后者会顺带真的开始索引。三按钮档不能沿用 `library-row-geometry` 的判据：那条要求「同一行」，而这里换行正是要的行为。新判据保留它守的全部内容（不溢出控件盒、不溢出行、不压住描述），把「一行」换成「每一行都贴住控件右缘」，再加两条它没有的：主 CTA（Build index）必须被排出可见尺寸且不被行裁切；以及**可读性**——说明文字的可用宽度不低于 150px，且平均每行不少于 12 个字符。两条下限缺一不可：只看宽度，一个宽盒子里一行一个词也能过；只看每行字数，一条窄缝里放两行短句也能过；而坏掉的那一档两条都不过。**现有断言与 1.5px 容差一字未改**，`library-row-geometry` 仍要求两按钮档在 448px 与 848px 一行右对齐。代价是明确的：那一档的说明文字仍只有 81px，因为在 448px 的面板里「两个按钮一行」和「文字有 150px」是数学上互斥的，而前者是锁住的。
- validation: 加断言先取红。修复前跑 `npm run test:desktop`，14 项 13 绿 1 红，红的原文是 `FAIL  library-row-three-buttons-geometry / narrow panel 448px (window 700): the description column is 6px wide, under the 150px a sentence needs to read as one; its 97 characters are spread over 13 lines — 7.5 characters a line, under 12`。改完 CSS 后 15/15 全绿，同一条读作 `narrow panel 448px (window 700): 3 lines, each right-aligned, Build index visible at 496.1..588, description 176px wide, 24.3 characters over 4 lines; wide panel 848px (window 1400): 1 line, each right-aligned, Build index visible at 1005.2..1097.1, description 406px wide, 48.5 characters over 2 lines`；`library-row-geometry` 与 `library-row-geometry-stacked` 两条旧断言的数字与修复前逐字相同。零 console / pageerror。`npm test` 全工作区绿（plugin 672 tests / 42 files）；`npm run test:release-tools` 229/229；`npm run check:boundaries` OK。截图新增 `library-row-three-buttons-narrow-panel.png` / `-wide-panel.png` / `-stacked-panel.png` 与 `personal-library-section-authorized.png`，均在 `.acceptance-out/desktop-acceptance/`。
- boundary: 只动样式与验收。ADR 0005 / 0008 不动，发现闭环 / email helm 不动。工作区保持未提交。
- next: 留一个可以由人判断的问题：两按钮档在 448px 面板下说明文字只有 81px（约每行 12 个字符，见 `library-row-narrow-panel.png`），读得了但很窄。要改善就得让这一档在 448px 也换行，那与现有 `library-row-geometry` 直接冲突，所以没有自行放宽——是否把那条断言的期望从「448px 一行」调整到某个更宽的档位，交由用户决定。

## 2026-08-30 — 可读下限升成统一不变式，「按钮一行」在冲突时让位

- evidence: 上一条 next 留给用户判断的问题有了结论，而且比问题本身更难看：三按钮状态在面板 448px 下换行、说明文字拿回 176px，两按钮状态在同一面板宽度下被 `library-row-geometry` 锁在一行，说明文字只有 81px、107 个字符排成 10 行（每行约 10.7 个字符）。对比 `.acceptance-out/desktop-acceptance/library-row-three-buttons-narrow-panel.png` 与 `library-row-narrow-panel.png` 一眼可见：**按钮越多这一行反而越好看**。这不是权衡出来的代价，是规则写歪了——可读下限只写进了三按钮那条断言，两按钮那条把「挤压」当成了期望值。
- change: 只改样式（外加下面那一条断言）。把上一轮按按钮个数写的两条 `max-width`（基础一条 + `:has(> button:nth-of-type(3))` 一条）收敛成一条，由**唯一的旋钮**推出来：`.setting-item:has(> .arxiv-daily-settings__library-controls)` 上声明 `--arxiv-daily-library-description-floor: 176px`，即「说明列最窄可以到多少还读得成一句话」。按钮组的上限写成 `max-width: calc(100% - var(--arxiv-daily-library-description-floor) - 16px)`（16px 是 Obsidian 两列之间的间距），说明列的下限写成 `min-width: var(--arxiv-daily-library-description-floor)`——同一个数从两头说一遍，两条规则不会各自漂移。`flex-shrink: 0` 的作用与上一轮相同：按钮组只有「自然一行宽」和「上限」两种宽度，放得下就一行，放不下就换行。**规则里不再出现按钮个数**：换不换行由「自然宽是否超过上限」决定，两按钮和三按钮走同一条路，将来第四个按钮也不用再加特例。旋钮取 176px 而不是判据的 150px，是留出余量——主题字体更宽或句子更长时还有地方可退。堆叠档（`@container (max-width: 340px)`）把上限和下限**一起**解除：那里 Obsidian 把行变成一列、按钮拉成整行宽，两列的交易本身不存在了，只留一半反而会把本该通栏的按钮缩窄。按钮逻辑、文案、状态判定一字未动。
- disposition: **本次唯一改动的既有断言是 `library-row-geometry`，方向是加严**。它原来对两按钮档跑 `judgeLibraryGeometry`（要求 448px 与 848px 都必须一行右对齐），现在改跑三按钮那套 `judgeLibraryWrappedGeometry`——保留它守的全部内容（不溢出控件盒、不溢出行、不压住描述），把「必须一行」换成「每一行都贴住控件右缘」，并加上它原来没有的两条：主 CTA 必须可见不被裁切，以及 `judgeDescriptionReadable`（说明列 ≥150px 且平均每行 ≥12 字符）。`judgeDescriptionReadable` 的 150px / 12 字符下限一个字没改，1.5px 容差一个字没改。其余断言（`library-row-geometry-stacked`、三按钮那两条、按钮文案、分组顺序、弹框时机、console 零错误）全部原样。`judgeLibraryGeometry` 本身保留但已无场景调用，doc comment 里写明了原因。同时更新了 `settings-tab.test.ts` 里三条对 `styles.css` 做正则的结构断言——它们锁的正是要拆掉的按钮个数特例，不改就自相矛盾；新的三条改锁「旋钮只声明一次且不低于 150px」「上限与下限都由旋钮推出」「样式里不存在任何按 `nth-of-type` 分档的规则」，比原来更严。
- **有意的行为变更，不是退化**：两按钮状态在面板 448px（窗口 700px）下现在**会换行**，排成 2 行、每行右对齐，说明文字从 81px 变成 176px、从 10.7 字符一行变成 26.8 字符一行。宽面板（848px）下两按钮与三按钮**都仍是一行右对齐**——那里空间充足，上限根本不咬。
- validation: 先只改断言不改样式取红。`npm run test:desktop` 15 项 14 绿 1 红，红的原文是 `FAIL  library-row-geometry / narrow panel 448px (window 700): the description column is 81px wide, under the 150px a sentence needs to read as one; its 107 characters are spread over 10 lines — 10.7 characters a line, under 12`——引用的是真实测量值，不是构造值。改完 CSS 后 15/15 全绿，同一条读作 `narrow panel 448px (window 700): 2 lines, each right-aligned, Build index visible at 496.1..588, description 176px wide, 26.8 characters over 4 lines; wide panel 848px (window 1400): 1 line, each right-aligned, Build index visible at 1081.1..1173, description 481px wide, 53.5 characters over 2 lines`。三按钮那条的数字与上一轮逐字相同（448px 三行 / 176px / 24.3 字符；848px 一行 / 406px / 48.5 字符），两条堆叠断言也未变。零 console / pageerror。`npm test` 全工作区绿（plugin 673 tests / 42 files）；`npm run test:release-tools` 229/229 + product units OK；`npm run check:boundaries` OK。截图全部重新生成在 `.acceptance-out/desktop-acceptance/`，`library-row-narrow-panel.png` 现在是两行按钮加一段读得了的说明。
- boundary: 只动样式、一条验收断言、以及与之直接矛盾的三条 CSS 文本单元测试。ADR 0005 / 0008 不动，按钮集合与状态判定不动，发现闭环 / email helm 不动。工作区保持未提交。
- next: 规则收敛完了，剩下的只有一个数：176px。要更宽的说明列就把它调大（按钮会在更宽的面板上就开始换行），要更晚换行就调小（下限 150px 是硬底，低于它验收会红）。不需要再碰选择器。

## 2026-08-30 — 披露框标题改直白并跟着深度走；验收的定位与文案断言解耦

- evidence: 披露框标题是固定的 `Authorize personal library`。它说的是这一步在系统里叫什么，不是这一步会发生什么——用户点开它之前不知道确认之后有内容离开本机。截图 `remote-full-text-disclosure-modal.png` 里正文已经把目的地、用途、可撤销都写清楚了，唯独最显眼的那行是行话。
- change: 标题改成直白的疑问句，并**按 `processingDepth` 取**：`full-text` 读作 `Send full text off this device?`，`metadata-and-abstracts` 读作 `Send titles and abstracts off this device?`。不写死「全文」是这次的关键：深度不是常量，`authorizeLibraryConnection` 与 `libraryAuthorizationDisclosure` 都按「有没有嵌入接口」二选一，把标题钉在全文上，就等于在另一种深度下造出**第三个披露失实缺陷**——前两个（depth 取自既有 grant 而非当前 scope、嵌入地址复用 chat 的 URL）刚在 P1d 修完，同一个坑不再踩一次。取值逻辑抽成 `libraryAuthorizationTitle(depth)`，与正文里那两处既有的深度分支同源。**确认按钮仍是 `Authorize`，正文、字段列表、那段说明、深度判定逻辑一字未动**。
- disposition: 顺带拆掉一个隐患：桌面验收原来用标题文本 `Authorize personal library` 去 `document.querySelectorAll(".modal-container .modal")` 里找那个框，找到了才能点它的 Cancel / Authorize。也就是说**定位依赖的是产品文案**，而文案是会改的——像这次这样改一次，验收就会红在「切下拉没有弹出披露框」上，读起来像同意机制坏了，实际只是标题换了词，报错指向完全错误的地方。改成给披露框根元素加一个稳定 class `arxiv-daily-library-authorization-modal`（沿用仓库既有的 `arxiv-daily-` 前缀，与 `arxiv-daily-hub-modal` 同法），验收按 class 定位，读到的标题只作为数据返回。**断言与定位就此分开**：新增判据 `judgeDisclosureTitle(modal, depth)` 拿深度对应的期望文案比对标题，于是文案改了红在「标题不符」上，框没弹才红在「找不到框」上，两种失败各自指得准。class 两头都有单测守着（改哪一头都红）：插件侧 `plugin/tests/library-modal.test.ts` 断言弹框根元素带这个 class，验收侧 `scripts/tests/desktop-acceptance-library-settings.test.mjs` 断言取数表达式里出现这个 class、且**不再对 `.modal-title` 做等值匹配**；两处都把字面量写死而不是从被测模块 import，否则断言会和实现自说自话。
- **metadata 深度的标题够不到桌面验收，在插件单测里覆盖**：设置页的 `requestRemoteFullTextConsent` 在 `disclosure.embeddingEndpoint` 缺失时直接返回 `undisclosable`，根本不开框——所以这个页面能弹出来的披露**只可能是 full-text 深度**。桌面验收走的是真实设置页，因此结构上到不了另一种深度；`library-modal.test.ts` 能直接调 `confirmLibraryAuthorization` 传入 metadata 深度的披露，两条标题各有一条断言。这一点写进了 `DISCLOSURE_TITLES` 的注释里，免得后来者以为是漏测。
- validation: 桌面验收由 15 项加到 16 项（新增 `remote-disclosure-title`），16/16 全绿，零 console / pageerror。**红的证据**：改完实现后把期望标题临时改回旧文案重跑，红的原文是 `FAIL  remote-disclosure-title / the disclosure is titled "Send full text off this device?", expected "Authorize personal library" at full-text depth`——红在标题不符，而同一次运行里 `remote-switch-asks-in-place`、`declined-remote-switch-changes-nothing`、`build-index-asks-before-remote-indexing` 与两条三按钮断言全部照常通过，而后三者都必须先找到那个框、点它的按钮才能成立，这正说明定位已经不吃文案了。`npm test` 全工作区绿（plugin 674 tests / 42 files）；`npm run test:release-tools` 235/235 + product units OK；`npm run check:boundaries` OK；plugin typecheck 通过。截图重新生成，`remote-full-text-disclosure-modal.png` 里标题已是 `Send full text off this device?`，正文与两个按钮与上一轮逐字相同。
- boundary: 只动标题取值与验收的定位方式。ADR 0005 / 0008 不动，确认按钮文案、正文、字段列表、深度判定逻辑不动，既有断言与 1.5px 容差一条未放宽。发现闭环 / email helm 不动。工作区保持未提交。
- next: 若将来出现第三种处理深度，`libraryAuthorizationTitle` 与 `DISCLOSURE_TITLES` 要一起加一条——后者缺项时 `judgeDisclosureTitle` 报「no heading is specified」而不是默默放过，这条已有单测。

## 2026-08-30 — 确认按钮改成回答标题的问题；「定位不依赖文案」从弹框推广到按钮

- evidence: 上一条把标题换成了跟深度走的问句（`Send full text off this device?`），确认按钮却仍是 `Authorize`。**问的和答的不是同一件事**：问句问「要不要把全文发出去」，肯定回答说的却是「授权」——换了词也换了概念，用户得自己把「授权」在心里翻译成「是，发出去」。截图 `remote-full-text-disclosure-modal.png` 里这一对就并排摆着，标题是大白话，按钮是行话。
- change: 确认按钮跟着同一个深度走，和标题用同一套词：`full-text` → `Send full text`，`metadata-and-abstracts` → `Send titles and abstracts`；取消按钮仍是 `Cancel`。关键在**取值只有一处**：上一轮的 `libraryAuthorizationTitle(depth)` 换成 `libraryAuthorizationCopy(depth)`，一次深度判断同时给出 `{ title, confirm }`，弹框两处文案都从它取。不做成两个各自 switch 深度的函数，是因为那样将来加第三种深度必然漏改一个——这正是标题上一轮要避免的「深度不是常量」那类坑，只是换到了按钮上。**正文、字段列表、结尾那段说明、深度判定逻辑一字未动**，`processingDepth` 的来源与语义不变。
- disposition: 顺带把上一轮那条教训补完。上一轮只把**弹框**的定位从「按标题文本找」改成了按稳定 class 找，**按钮仍然是按文案点的**：`clickModalButtonExpression("Cancel")` / `clickModalButtonExpression("Authorize")` 在弹框里 `find` 文本相等的 button。也就是说改按钮文案会直接弄坏验收，而报的错是「the modal has no Authorize button」——读起来像按钮没了，实际只是换了词。**和标题那次一模一样的失败模式，只是从弹框挪到了按钮**。现在两个按钮各带一个稳定 class（沿用 `arxiv-daily-` 前缀，与弹框那个 class 同法）：`arxiv-daily-library-authorization-confirm` 与 `arxiv-daily-library-authorization-cancel`；`clickModalButtonExpression(buttonClass)` 收的是标识不是文案，`modalExpression()` 把两个按钮的文字当**数据**读出来。**定位与断言就此分开**：新增判据 `judgeDisclosureButtons(modal, depth)` 拿 `DISCLOSURE_CONFIRM_LABELS[depth]` 比对确认按钮文字、拿 `DISCLOSURE_CANCEL_LABEL` 比对取消按钮，文案改了红在「按钮文案不符」，按钮真没了才红在「找不到那个 class 的按钮」，两种失败各自指得准（各有单测分别守着，且断言互相排斥：文案不符那条 `doesNotMatch(/has no|not found|missing/i)`）。两个 class 两头都有单测写死字面量：插件侧 `plugin/tests/library-modal.test.ts` 断言两个按钮各自带 class、且确认按钮文字另行断言；验收侧 `scripts/tests/desktop-acceptance-library-settings.test.mjs` 断言点击表达式里出现这两个 class、且**不再对按钮 `textContent` 做等值匹配**、也不再出现任何按钮文案字面量。
- **标题与按钮同源，有单测把这条关系钉住**：`plugin/tests/library-modal.test.ts` 里一条测试对两种深度断言 `title === confirm + " off this device?"`，验收侧对 `DISCLOSURE_TITLES` / `DISCLOSURE_CONFIRM_LABELS` 断言同一条等式。加第三种深度时若只补一边，这两条会红。
- **metadata 深度的按钮文案够不到桌面验收，在插件单测里覆盖**：原因与上一轮标题完全相同——`requestRemoteFullTextConsent` 在没有嵌入接口可披露时返回 `undisclosable`、根本不开框，设置页能弹出来的披露只可能是 full-text 深度。`library-modal.test.ts` 直接调 `confirmLibraryAuthorization` 传 metadata 深度，断言确认按钮读作 `Send titles and abstracts`；这一点写进了 `DISCLOSURE_CONFIRM_LABELS` 的注释里。
- validation: 桌面验收由 16 项加到 17 项（新增 `remote-disclosure-buttons`），17/17 全绿，零 console / pageerror。**红的证据**：实现完成后把 `DISCLOSURE_CONFIRM_LABELS["full-text"]` 临时改回 `Authorize` 重跑，红的原文是 `FAIL  remote-disclosure-buttons / the confirm button reads "Send full text", expected "Authorize" at full-text depth`——红在**按钮文案不符**，不是「找不到按钮」。同一次运行里 `remote-switch-asks-in-place`、`remote-disclosure-title`、`declined-remote-switch-changes-nothing`、`build-index-asks-before-remote-indexing` 与两条三按钮断言全部照常通过，而后三者都必须先找到那个框、**点它的 Cancel / 确认按钮**才能成立（三按钮状态是靠点确认按钮授权出来的），这正说明按钮定位已经不吃文案了。`npm test` 全工作区绿（plugin 677 tests / 42 files）；`npm run test:release-tools` 274/274 + product units OK；`npm run check:boundaries` OK；`npm run typecheck` 通过；`npm run lint` 0 error。截图重新生成，`.acceptance-out/desktop-acceptance/remote-full-text-disclosure-modal.png` 里两个按钮已是 `Cancel` / `Send full text`，标题与正文与上一轮逐字相同。
- boundary: 只动确认按钮文案与按钮的定位方式。标题、正文、字段列表、深度判定逻辑不动；ADR 0005 / 0008 不动；既有断言与 1.5px 容差一条未放宽（`remote-disclosure-buttons` 是净增的一条）。发现闭环 / email helm 不动。工作区保持未提交。
- next: 第三种处理深度到来时要同时补三处：`libraryAuthorizationCopy` 的分支、`DISCLOSURE_TITLES`、`DISCLOSURE_CONFIRM_LABELS`。后两者缺项时判据分别报「no heading is specified」/「no confirm label is specified」而不是默默放过，都已有单测。
