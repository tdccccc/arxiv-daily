# P3 — end-to-end verification

goal_ref: ../goal.md
status: done

## Outcome

The complete scientific Markdown change set is audited, verified in a test vault, and ready for the initiative to close.

## Assumptions

- Focused P1 and P2 coverage accurately represents the production report and persistence paths.
- A disposable test vault and the normal local plugin deployment workflow are available for manual verification.

## Approach

Adjust the audited compatibility contracts in place, run final automated verification, and leave deployment plus any rerun as explicit later actions. Deployment may target only the designated test vault. Existing report inspection or rescan is read-only; rerunning report generation is a manual user action only.

## Tasks

- [x] Resolve the independent-audit findings for CommonMark-aware HTML neutralization, historical heading identity, selection markers, and rescue normalization.
- [x] Run focused rendering/parser/rescue/selection/pipeline/writer/history tests, core/plugin typechecks, and repository checks.
- [x] Audit the combined P1/P2 and preserved fallback diff for scope, compatibility, and accidental data/settings exposure.
- [x] Build and deploy the plugin only to the designated test vault using the repository's established workflow when separately authorized.
- [x] Preserve the existing mixed structured/fallback scientific report unchanged for later manual source/rendered inspection; do not automate report generation or rescan.
- [x] Record evidence and close the initiative after automated verification and byte-for-byte deployment checks pass.

## Verification

- Final automated checks pass from the feature worktree.
- Test-vault source and rendered views preserve representative MathJax/comparisons exactly and retain correct block classification.
- `/home/tiandc/Documents/code/arxiv-daily/plugin_test/arxiv-daily/daily/2026-07-21.md` is never modified or regenerated by automation; it may only be inspected or rescanned read-only, and rerun is manual user action only.
- No production vault, `data.json`, settings, commit, or remote branch is modified.

## Abort / reshape triggers

- If the designated test vault or deployment target is ambiguous, stop before deployment and ask for the target.
- If end-to-end behavior differs from focused tests, stop and classify the evidence before changing production code.
